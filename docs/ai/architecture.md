# Public distribution architecture

This public repository distributes reviewed Windows artifacts and public instructions sourced from `Mouse-ly/DesktopApp`. It must not contain the private application source or internal Mousely context. Every artifact must identify its source commit, Windows package/version identity, checksum or signature, and supported updater window.
