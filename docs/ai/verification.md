# Verification

Run contract/context verification, `node .mousely/public-leakage-check.mjs .`, and `git diff --check`. For artifact changes, use Windows CI to verify download, checksum/signature, installer/package identity, and updater compatibility. Publication or replacement of a public artifact requires the human gate.
